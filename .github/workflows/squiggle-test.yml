name: Squiggle Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch Squiggle tips
        run: |
          curl -s -H "User-Agent: github-actions" "https://api.squiggle.com.au/?q=tips;year=2026;source=1;format=json"
